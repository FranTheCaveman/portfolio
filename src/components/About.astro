<main>
    <div class="container">
        <iframe 
            src={`${import.meta.env.BASE_URL}solitaire/mygame.html`}
            allow="gamepad"
            allowfullscreen
            scrolling="no">
        </iframe>
    </div>
</main>

<style>
    main {
        width: 100%;
        height: 100%;
        margin: 0 auto;
        top: 0;
        position: relative;
    }

    iframe {
        width: 100%;
        height: 90%;
        border: none;
        overflow: hidden;
    }

    html, body {
        margin: 0;
        padding: 0;
        width: 100%;
        height: 100%;
        overflow: hidden; /* Stops the browser from scrolling */
        position: fixed;  /* Anchors the workspace on mobile devices */
        touch-action: none; /* Prevents default zoom/scroll actions on touch */
    }
</style>